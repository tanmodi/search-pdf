# readme.md

# To run the code open two terminal

In first terminal

```bash
cd backend
npm install 
npm i -D

npm run start
```

in second terminal 

```bash
cd front
npm install
npm i -D

npm run next
```